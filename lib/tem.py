# このファイルは formatting_data.py の実装検討用の一時ファイルでした
# 実装完了により不要になりました



def 隠して押したら開くようにする():
  return html

class FileClass:
  include_resource_types = [
      {
        "type": "aws_iam_role",
        "priority": 1,
        "view_type": "list",
        "special_parameters": [
           {
              "name": "assume_role_policy"
              "function": 隠して押したら開くようにする
           }
        ]

        }"
      },
      {...}
  ]
  file_path = "aws/iam/role.html""